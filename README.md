# Trend Signals

Поиск зарождающихся технологических трендов. Команда READY2HYPE, хакатон «Лидеры
цифровой трансформации» 2026, задача №1 (Газпромбанк.Тех).

Пользователь вводит технологическое направление и получает 15 зарождающихся трендов:
чем тренд полезен, кто им занимается, ссылки на первоисточники.

## Установка

Нужен только `uv` — он сам скачает нужную версию Python, создаст окружение и поставит
зависимости точных версий. Системный Python при этом не затрагивается.

```powershell
winget install --id astral-sh.uv
```

Если winget недоступен: `irm https://astral.sh/uv/install.ps1 | iex`
На macOS и Linux: `curl -LsSf https://astral.sh/uv/install.sh | sh`

Дальше:

```
git clone https://github.com/READY2HYPE/trend-signals-lct2026.git
cd trend-signals-lct2026
uv sync
copy .env.example .env
```

В `.env` вписать почтовый адрес — он нужен для вежливого режима OpenAlex, без него API
ограничивает скорость выгрузки.

Запуск через `uv run`, активировать окружение вручную не требуется:

```
uv run python -V
uv run pytest
```

Библиотеки машинного обучения весят гигабайты и нужны не всем, поэтому ставятся отдельно:

```
uv sync --extra ml
```

## Структура

```
config/   параметры среза данных
data/     подвыборка, агрегаты, манифест (полная выгрузка — вне репозитория)
docs/     документация
```

## Документация

| | |
|---|---|
| [Задача](docs/01-задача.md) | текст заказчика, сроки, что из него следует |
| [Методология](docs/02-методология.md) | что считаем трендом, как считаем, как проверяем |
| [Данные](docs/03-данные.md) | источники, правила отбора, хранение, эталон для проверки |
| [План работ](docs/04-план-работ.md) | объём, роли, задачи, открытые вопросы, риски |
| [Разработка](docs/05-разработка.md) | git, тестирование, воспроизводимость |
| [ТЗ: сбор корпуса](docs/06-тз-сбор-корпуса.md) | текущий этап и критерии приёмки |

## Состояние

Подготовка. Полный текст задания открывается 15.09 вместе со стартом разработки; до этого
собирается корпус, который понадобится при любой формулировке.
